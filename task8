# Задание 1

# Считываем количество чисел
N = int(input())

# Считываем числа в список
numbers = []
for _ in range(N):
    number = int(input())
    numbers.append(number)

# Переворачиваем список
numbers.reverse()

# Выводим перевернутый список
print(*numbers)

# Задание 2

# Считываем количество элементов N
N = int(input())

# Считываем массив чисел и преобразуем в список
numbers = list(map(int, input().split()))

# Изменяем порядок элементов, как требуется
result = numbers[-1::-2] + numbers[-2::-2]

# Выводим результат
print(*result

# Задание 3

# Чтение входных данных
m = int(input())  # Максимальная масса, которую выдерживает лодка
n = int(input())  # Количество рыбаков
weights = [int(input()) for _ in range(n)]  # Веса рыбаков

# Сортируем рыбаков по весу
weights.sort()

# Инициализация указателей
i, j = 0, n - 1  # i - на самого лёгкого, j - на самого тяжёлого
boats = 0  # Счётчик лодок

# Пока указатели не пересеклись
while i <= j:
    # Если самый лёгкий и самый тяжёлый помещаются в одну лодку
    if weights[i] + weights[j] <= m:
        i += 1  # Убираем самого лёгкого
    # В любом случае сажаем самого тяжёлого
    j -= 1
    boats += 1  # Лодка использована

# Выводим результат - минимальное количество лодок
print(boats)
